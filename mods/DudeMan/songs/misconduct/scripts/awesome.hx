function create() {
}

function stepHit(curStep:Int) { //this is all template btw lol so
    switch (curStep) {
        case 64: 
        defaultCamZoom = 1.6;
        case 320: 
        defaultCamZoom = 1;
        case 768: 
        defaultCamZoom = 1.6;
        }
}