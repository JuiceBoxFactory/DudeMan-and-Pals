function create() {
}

function stepHit(curStep:Int) { //this is all template btw lol so
    switch (curStep) {
        case 0:
       defaultCamZoom = 1.6; 
        case 896:
       defaultCamZoom = 1; 
        case 1020:
       defaultCamZoom = 1.6; 
	}
}