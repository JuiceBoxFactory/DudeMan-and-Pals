function create() {

//	boyfriend.scale.set(2.3,2.3);
//	boyfriend.x += 650;
//	boyfriend.y += 50;
//	boyfriend.cameraOffset.set(-1250,0);
	
}

function onDadHit(event) {
	if (881 < curStep) {
		if (health > 0.1) {
		health -= 0.02;
			}
		}
}
